using System.Collections;
using System.Collections.Generic;
using EMP.Core;
using UnityEngine;

public class MonopoleOwnershipManager : Singleton<MonopoleOwnershipManager>
{
    public uint simulatingClient = 1;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
